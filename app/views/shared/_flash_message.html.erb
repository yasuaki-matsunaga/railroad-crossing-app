<% flash.each do |message_type, message| %>
  <div class="alert alert-<%= message_type %> d-flex align-items-center justify-content-center  flash-message shadow z-3">
    <%= message %>
  </div>
<% end %>